<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TravelPlanner">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.TravelPlanner.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".TripDetailActivity" android:exported="false"
            android:theme="@style/Theme.TravelPlanner.NoActionBar"/>
        <activity android:name=".AddScheduleActivity" android:exported="false"
            android:theme="@style/Theme.TravelPlanner.NoActionBar"/>
        <activity android:name=".PhotoGalleryActivity" android:exported="false"
            android:theme="@style/Theme.TravelPlanner.NoActionBar"/>
        <activity android:name=".DiaryActivity" android:exported="false"
            android:theme="@style/Theme.TravelPlanner.NoActionBar"/>

    </application>
</manifest>